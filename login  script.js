


  function submitLoginForm() {
    var email = document.getElementById("email-input").value;
    var password = document.getElementById("password-input").value;
  
    // Perform your validation logic here
    // Replace the following condition with your actual validation logic
    if (email === "user1@holdings.com" && password === "admin") {
      // Redirect to the private.html page upon successful login
      window.location.href = "private1.html";
    } else {
      // Display an error message or perform other actions for failed login
      alert("Invalid email or password. Please try again.");
    }
  }


  function submitLoginForm() {
    var email = document.getElementById("email-input").value;
    var password = document.getElementById("password-input").value;
  
    // Perform your validation logic here
    // Replace the following condition with your actual validation logic
    if (email === "user2@holdings.com" && password === "admin") {
      // Redirect to the private.html page upon successful login
      window.location.href = "dashboard/private2.html";
    } else {
      // Display an error message or perform other actions for failed login
      alert("Invalid email or password. Please try again.");
    }
  }





//private 3 origional
  function submitLoginForm() {
    var email = document.getElementById("email-input").value;
    var password = document.getElementById("password-input").value;
  
    // Perform your validation logic here
    // Replace the following condition with your actual validation logic
    if (email === "user3@holdings.com" && password === "admin") {
      // Redirect to the private.html page upon successful login
      window.location.href = "dashboard/private3.html";
    } else {
      // Display an error message or perform other actions for failed login
      alert("Invalid email or password. Please try again.");
    }
  }
  

//private 4
function submitLoginForm() {
  var email = document.getElementById("email-input").value;
  var password = document.getElementById("password-input").value;

  // Perform your validation logic here
  // Replace the following condition with your actual validation logic
  if (email === "user4@holdings.com" && password === "admin") {
    // Redirect to the private.html page upon successful login
    window.location.href = "dashboard/private4.html";
  } else {
    // Display an error message or perform other actions for failed login
    alert("Invalid email or password. Please try again.");
  }
}
 


//private 5
function submitLoginForm() {
  var email = document.getElementById("email-input").value;
  var password = document.getElementById("password-input").value;

  // Perform your validation logic here
  // Replace the following condition with your actual validation logic
  if (email === "user5@holdings.com" && password === "admin") {
    // Redirect to the private.html page upon successful login
    window.location.href = "dashboard/private5.html";
  } else {
    // Display an error message or perform other actions for failed login
    alert("Invalid email or password. Please try again.");
  }
}
 


//private 6
function submitLoginForm() {
  var email = document.getElementById("email-input").value;
  var password = document.getElementById("password-input").value;

  // Perform your validation logic here
  // Replace the following condition with your actual validation logic
  if (email === "user6@munjholdings.com" && password === "admin") {
    // Redirect to the private.html page upon successful login
    window.location.href = "dashboard/private6.html";
  } else {
    // Display an error message or perform other actions for failed login
    alert("Invalid email or password. Please try again.");
  }
}
 